# History Layout

This folder keeps repository history artifacts out of the project root.

- `history/development/`: implementation plans and development notes.
- `history/logs/`: seed/runtime logs and state snapshots.
